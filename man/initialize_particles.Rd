% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initialize_particles.R
\name{initialize_particles}
\alias{initialize_particles}
\title{初始化粒子群（4PL物流调度多目标优化）}
\usage{
initialize_particles(pop_size, tasks, resource_mapping)
}
\arguments{
\item{pop_size}{正整数，粒子群规模（建议取值：50-200，根据问题复杂度调整）。}

\item{tasks}{列表，物流任务集合（同\code{decode_schedule}的\code{tasks}参数）。}

\item{resource_mapping}{列表，自有资源到外包资源的映射（如\code{M1 = "O1"}）。}
}
\value{
列表，长度为\code{pop_size}，每个元素为粒子对象，包含：
\itemize{
\item \code{position}：列表，子任务名称到资源名称的映射（核心决策变量）；
\item \code{velocity}：列表，子任务的资源选择速度（初始为0）；
\item \code{best_position}：列表，粒子的历史最优位置（初始为当前位置）；
\item \code{fitness}：数值向量，当前位置的适应度（初始为无穷大）；
\item \code{best_fitness}：数值向量，历史最优适应度（初始为无穷大）。
}
}
\description{
针对第四方物流（4PL）的资源调度多目标优化问题，该函数根据任务优先级和资源类型（自有/外包）
初始化粒子群，每个粒子代表一个资源分配方案，包含位置、速度、历史最优位置等属性。
}
\details{
该函数是粒子群优化（PSO）算法的初始化模块，根据任务优先级进行资源选择偏向：
高优先级任务（优先级3）优先选择外包资源（O开头），其他优先级任务优先选择自有资源（M开头），
保证初始解的合理性和算法收敛速度。
}
\examples{
# 构建示例数据
resource_mapping <- list(M1 = "O1", M2 = "O2")
tasks <- list(
  task1 = list(
    priority = 2,
    due_date = 10,
    delay_penalty = 5,
    activities = list(
      list("subtask1_1", list(M1 = c(2.0, 10.0, 2), O1 = c(1.0, 20.0, 2))),
      list("subtask1_2", list(M2 = c(3.0, 15.0, 3), O2 = c(2.0, 25.0, 3)))
    ),
    relationships = c(0)
  ),
  task2 = list(
    priority = 3,
    due_date = 15,
    delay_penalty = 3,
    activities = list(
      list("subtask2_1", list(M1 = c(2.0, 10.0, 2), O1 = c(1.0, 20.0, 2))),
      list("subtask2_2", list(M2 = c(3.0, 15.0, 3), O2 = c(2.0, 25.0, 3)))
    ),
    relationships = c(0)
  )
)

# 初始化粒子群（规模为2）
particles <- initialize_particles(pop_size = 2, tasks = tasks, resource_mapping = resource_mapping)

# 查看第一个粒子的位置
str(particles[[1]]$position)

}
\references{
\enumerate{
\item Kennedy, J., & Eberhart, R. (1995). Particle swarm optimization. Proceedings of ICNN'95 - International Conference on Neural Networks, 4, 1942-1948.
\item Coello, C. A. C., Lechuga, M. S. (2002). MOPSO: A proposal for multiple objective particle swarm optimization. IEEE Transactions on Evolutionary Computation, 8(3), 256-279.
}
}
